import { DocumentsByMonthPipe } from './documents-by-month.pipe';

describe('DocumentsByMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentsByMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
