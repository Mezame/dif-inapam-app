import { SingleFormErrorPipe } from './single-form-error.pipe';

describe('SingleFormErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new SingleFormErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
